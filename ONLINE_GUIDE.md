# 绿茵禁选对决 - 在线对战使用指南

## 快速开始

### 1. 启动服务器

```bash
cd "c:\Users\29576\Desktop\足球游戏"
npm start
```

服务器启动后显示:
- 本机访问: http://localhost:3000
- 局域网地址: http://[你的IP]:3000 (可供同一网络下的设备访问)

### 2. 本机对战测试

1. 打开浏览器访问 http://localhost:3000
2. 点击 "好友在线对战"
3. 输入昵称，点击 "创建房间"
4. 复制房间码或邀请链接
5. 在同一浏览器的**新标签页**打开 http://localhost:3000
6. 输入不同昵称，输入房间码，点击 "加入房间"
7. 双方连接后自动开始对战

### 3. 与好友远程对战

#### 方法一：部署到公网服务器

1. 将整个项目部署到具有公网IP的服务器
2. 在服务器上运行 `npm start`
3. 分享服务器地址给好友

#### 方法二：使用内网穿透（推荐）

推荐使用 [ngrok](https://ngrok.com/) 或 [frp](https://github.com/fatedier/frp) 进行内网穿透：

```bash
# 使用 ngrok
ngrok http 3000
# 复制生成的 https 地址分享给好友
```

#### 方法三：同一局域网

如果好友在同一WiFi网络下:
1. 确认电脑的局域网IP地址
2. 好友访问 http://[你的IP]:3000
3. 直接创建/加入房间

## 功能说明

### 在线对战规则

- **房间创建**: 房主创建房间后获得6位房间码
- **邀请好友**: 发送房间码或邀请链接给好友
- **30秒限时**: 每步操作限时30秒，超时自动执行
- **断线重连**: 掉线后房间保留90秒，可自动重连
- **轮换先手**: 每轮先禁权在房主和访客间交替

### 操作说明

1. **选择先手**: 每轮开始时决定先禁或后禁
2. **禁选球员**: 点击球员卡片，然后点击"确认禁用"
3. **选择球员**: 点击球员卡片，然后点击"确认选择"
4. **交换位置**: 在阵容调整阶段点击两个非门将槽位交换

## 技术细节

### WebSocket 连接

服务器通过 WebSocket (`ws://host/ws`) 进行实时通信:
- 房间创建/加入
- 实时同步禁选状态
- 倒计时同步
- 断线重连

### 协议版本

当前协议版本: 1

如遇到 "协议版本不一致" 错误，请刷新页面。

### 环境变量

```bash
PORT=3000           # 服务器端口
ALLOWED_ORIGINS=*   # 允许的来源（用逗号分隔）
```

示例：
```bash
ALLOWED_ORIGINS=http://localhost:3000,https://my-game.com npm start
```

## 故障排除

### 无法连接服务器
- 确认服务器已启动 (检查终端输出)
- 检查防火墙设置
- 确认使用的是正确的地址

### 房间码无效
- 房间码为6位数字
- 房间可能已过期（10分钟无活动自动删除）

### 对方掉线
- 系统会等待90秒重连
- 超时后房间自动结束

## 部署到云服务器

推荐使用以下云服务:

1. **Railway** - 免费额度，支持 Node.js
2. **Render** - 免费计划，支持 WebSocket
3. **Heroku** - 免费 dyno
4. **腾讯云/阿里云** - 国内服务器，延迟更低

部署时确保:
- Node.js >= 20
- 设置 `PORT` 环境变量
- 如有跨域需求，设置 `ALLOWED_ORIGINS`
